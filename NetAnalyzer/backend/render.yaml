services:
  - type: web
    name: cybersec-api
    env: node
    buildCommand: "npm install && pip3 install -r parser/requirements.txt"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
